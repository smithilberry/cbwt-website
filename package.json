{
  "name": "cbwt-website",
  "version": "0.0.2",
  "description": "Website for campbikewinetour.com",
  "author": "A Nomadic Collective Developers",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "parcel html/*.html --no-cache --out-dir build/dev",
    "build": "parcel build html/*.html --no-cache --out-dir build/dist",
    "purge": "rm -rf node_modules && yarn cache clean",
    "heroku-postbuild": "yarn build"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3"
  }
}
